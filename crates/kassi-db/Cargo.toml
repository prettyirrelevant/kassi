[package]
name = "kassi-db"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "0.8", features = ["postgres", "runtime-tokio", "tls-rustls", "migrate"] }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }

[lints]
workspace = true
